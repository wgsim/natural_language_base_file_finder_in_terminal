name: askfind_env
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - -r requirements.txt
      - pytest==9.0.2
      - pytest-cov==7.0.0
      - mypy==1.11.2
      - ruff==0.6.9
      - pip-audit==2.10.0
      - -e .
